protocols { imap = yes; pop3 = yes; }
mail_location = maildir:~/Maildir
ssl = no
disable_plaintext_auth = no
mail_privileged_group = mail
first_valid_uid = 1000
namespace inbox { inbox = yes; separator = /; }
passdb { driver = pam; }
userdb { driver = passwd; }
