# Legacy module — functions moved to:
# - services/email_service.py  (get_inbox, sync_emails_with_db)
# - services/system_service.py (create_system_user)
# - validators.py              (sanitize_username, is_valid_username)
